# template, target default on arm: a conjunction, applied after target/default and arch/arm
# (axes nest in the order target, toolchain, arch, platform, version, option)
CONF_FLAGS+=" --disable-neon"
