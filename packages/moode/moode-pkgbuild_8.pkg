# moode-pkgbuild_8
MOODEVER=8.1.0
MOODEREL=v$(echo ${MOODEVER} | sed 's/\.//g')
MOODEDEVCOMMIT=1410ac8c6db1a3eedb80e22514b2d9fc395ca602
if [ -z "${MOODEDEVCOMMIT}" ]
then
	PKG_URL="https://github.com/moode-player/pkgbuild/archive/refs/tags/${MOODEREL}.tar.gz"
else
	PKG_URL="https://github.com/moode-player/pkgbuild/archive/${MOODEDEVCOMMIT}.tar.gz"
fi
BUILD_PROCESS=downloadonly
